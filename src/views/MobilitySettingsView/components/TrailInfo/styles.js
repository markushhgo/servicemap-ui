const styles = theme => ({
  paragraph: {
    textAlign: 'left',
    padding: theme.spacing(1.5),
    width: '85%',
    marginLeft: theme.spacing(3),
  },
  margin: {
    marginTop: theme.spacing(1),
  },
});

export default styles;
