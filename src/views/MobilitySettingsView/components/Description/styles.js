const styles = theme => ({
  paragraph: {
    textAlign: 'left',
    padding: theme.spacing(1.5),
    width: '85%',
  },
  subtitle: {
    textTransform: 'none',
  },
  margin: {
    marginBottom: theme.spacing(1),
  },
});

export default styles;
